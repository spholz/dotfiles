[user]
    email = {{ .email }}
    name = {{ .name }}
[init]
    defaultBranch = main
[pull]
    ff = only
[merge]
    tool = nvimdiff2
    guitool = kdiff3   pushInsteadOf = kde:

[url "https://github.com/"]
    insteadOf = gh:
[url "ssh://git@github.com:"]
    pushInsteadOf = gh:

[url "https://gitlab.com/"]
    insteadOf = gl:
[url "ssh://git@gitlab.com:"]
    pushInsteadOf = gl:

[url "https://invent.kde.org/"]
    insteadOf = kde:
[url "ssh://git@invent.kde.org:"]
