vim.filetype.add {
    extension = {
        -- glsl (OpenGL)
        vert = 'glsl',
        frag = 'glsl',

        -- wgsl (WebGPU)
        wgsl = 'wgsl',

        -- qml (Qt Quick)
        qml = 'qmljs',
    },
}
